{
  "name": "gvo-skills-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing the gvo-skills repository to claude.ai over HTTP. Replaces the from-desktop MCP for the gvo-router skill.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.85.0"
  }
}
