# Missing Source Repos

Only OpenSpace (Python skill_engine port reference for Worker) is not yet cloned.
Not blocking — Step 10 (Worker deploy) is deferred and only needs OpenSpace as reference,
not a skill source.

If you want it later, provide the owner/repo and run:
  gh repo clone <owner>/<repo> /home/lando555/OpenSpace
